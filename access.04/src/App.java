import example.*;

public class App {
    public static void main(String[] args) {
        TestUser testUser = new TestUser();
        Test test = new Test();
        // test.performProtectedTest(); // not accessible outside example
        // package or class which extends Test
    }
}
